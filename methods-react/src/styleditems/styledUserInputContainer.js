import styled from "styled-components";

const StyledUserInputContainer = styled.div`
    display: flex;
    width:100%;
    color: ${(props) => props.theme.color};
`;

export default StyledUserInputContainer;