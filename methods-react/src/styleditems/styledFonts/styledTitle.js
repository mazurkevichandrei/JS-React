import styled, {css} from "styled-components";
import StyledListName from './styledListName';

const StyledTitle = styled(StyledListName)`
    font-size: 27px;
    margin: 10px 0;
`;

export default StyledTitle