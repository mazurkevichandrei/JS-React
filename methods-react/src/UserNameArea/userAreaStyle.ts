interface Styles {
    [key: string]: string
}

const userAreaStyles: Styles = {
    display: 'flex',
    maxWidth: '500px',
    alignItems: 'center',
    justifyContent: 'center',
}

export default userAreaStyles;