interface Styles {
    [key: string]: string
}

const userAreaStyles: Styles = {
    display: 'flex',
    maxWidth: '500px',
    alignItems: 'center',
    justifyContent: 'center',
    margin: '0',
    padding: '0',
}

export default userAreaStyles;